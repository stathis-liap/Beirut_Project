# generated pipeline outputs (regenerate via build_dem.py / flood_sim.py / render_3d.py)
/terrain/
/sim/
/output/

# python
__pycache__/
*.pyc

# numba disk cache
*.nbi
*.nbc

# venv
.venv/
venv/

# editor/tool noise
tempCodeRunnerFile.py

# large survey / CAD source data - multi-GB, kept out of git.
# obtain separately and drop into these folders; see README.
/data/
/3D/**
# ...but keep the tiny text sidecars that scripts and docs rely on
!/3D/**/
!/3D/**/*.tfw
!/3D/**/*.json
